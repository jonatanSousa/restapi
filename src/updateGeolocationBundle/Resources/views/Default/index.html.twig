{% extends 'base.html.twig' %}

{% block body %}
<p>Application Environment: {{ app.environment }}</p>

    <form>

        {{ form_start(form, { att:{class: 'form-inline'} })}}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-default"> Search </button>
        {{ form_errors(form.search) }}
        {{ form_end(form) }}

    </form>

{% endblock %}